# Optional future adapter configuration. The core does not read environment variables.
# Public endpoint: rate limited; choose a provider for production workloads.
ROBINHOOD_RPC_URL=https://rpc.mainnet.chain.robinhood.com/
# No registry is deployed. Leave this empty. No wallet or private key is required.
GRYMO_REGISTRY_ADDRESS=
